{
  "name": "video-redirector",
  "version": "1.0.0",
  "description": "A service to redirect video links with caching",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",  
    "start": "vercel --prod" 
  },
  "dependencies": {
    "axios": "^1.5.0",  
    "node-cache": "^5.1.2"  
  },
  "engines": {
    "node": "22.x"  
  }
}
